<table>
    <tr>
      <th>Name</th>
      <th>Age</th>
    </tr>
    <% for (let i = 0; i < users.length; i++) { %>
    <tr>
      <td><%= users[i].name %></td>
      <td><%= users[i].age %></td>
    </tr>
    <% } %>
  </table>
  